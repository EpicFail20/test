-  cp = Copy-Item
- rm = Remove-Item
- cd = Set-Location
- mkdir = New-Item
- man = Get-Help
- history = Get-History
- alias = Get-Alias ou New-Alias
- cat = Get-Content

- Toutes les commandes ont été trouvées avec la commande Get-Help suivie de la commande bash
